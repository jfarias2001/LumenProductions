version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - internal
    restart: unless-stopped
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER}']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - redis_data:/data
    networks:
      - internal
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 3s
      retries: 5

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      REDIS_URL: redis://redis:6379
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      AI_DEFAULT_MODEL: ${AI_DEFAULT_MODEL:-gpt-4o-mini}
      CORS_ORIGIN: https://${DOMAIN}
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - internal
    restart: unless-stopped

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    depends_on:
      - api
    networks:
      - internal
      - traefik
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik_default"
      # HTTP -> redireciona para HTTPS
      - "traefik.http.routers.lumenproductions-http.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.lumenproductions-http.entrypoints=web"
      - "traefik.http.routers.lumenproductions-http.middlewares=lumenproductions-https-redirect"
      - "traefik.http.middlewares.lumenproductions-https-redirect.redirectscheme.scheme=https"
      - "traefik.http.middlewares.lumenproductions-https-redirect.redirectscheme.permanent=true"
      # HTTPS com TLS via leresolver (Let's Encrypt)
      - "traefik.http.routers.lumenproductions.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.lumenproductions.entrypoints=websecure"
      - "traefik.http.routers.lumenproductions.tls=true"
      - "traefik.http.routers.lumenproductions.tls.certresolver=leresolver"
      - "traefik.http.services.lumenproductions.loadbalancer.server.port=80"

networks:
  internal:
    driver: bridge
  traefik:
    name: traefik_default
    external: true

volumes:
  postgres_data:
  redis_data:
