# Domínio
DOMAIN=mkt.paglamp.com.br

# Banco de dados
POSTGRES_USER=content_engine
POSTGRES_PASSWORD=GERE_SENHA_FORTE_AQUI
POSTGRES_DB=content_engine

# JWT — gere com: openssl rand -hex 32
JWT_ACCESS_SECRET=GERE_64_CHARS_ALEATORIOS_AQUI
JWT_REFRESH_SECRET=GERE_64_CHARS_ALEATORIOS_AQUI_DIFERENTE

# OpenAI (opcional na v1 sem módulo de IA ativo)
OPENAI_API_KEY=sk-...
AI_DEFAULT_MODEL=gpt-4o-mini

# Traefik ACME (usado apenas no docker-compose.traefik.yml)
ACME_EMAIL=joaopaulo909909@gmail.com
